export enum eFrameType {
  beam,
  column,
  brace
}

export enum eSectionType {
  I_shape,
  Channel,
  DoubleChannel,
  T_shape,
  Angle,
  DoubleAngle,
  Rectangular,
  Circular,
  Box,
  Pipe,
  General
}